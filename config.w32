// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("stringutils", "for stringutils support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("stringutils", "enable stringutils support", "no");

if (PHP_STRINGUTILS != "no") {
	EXTENSION("stringutils", "stringutils.c");
}

